// Middleware testing is better done through E2E tests
// due to Next.js internal complexities

describe('Middleware', () => {
  it('should be tested via E2E tests', () => {
    // See e2e/auth.spec.ts for middleware behavior tests
    expect(true).toBe(true)
  })
})